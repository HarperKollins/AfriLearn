version: '3.8'

services:
  api:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8080:8080"
    environment:
      - PORT=8080
      - APP_ENV=production
      - DB_URL=postgresql://neondb_owner:npg_LfjKd3qP2wGk@ep-polished-thunder-aiss16k9.c-4.us-east-1.aws.neon.tech/neondb?sslmode=require
    restart: always
